#include <stdio.h>

int main() {
    char ch;

    // Iterate through the characters from 'A' to 'Z'
    for (ch = 'A'; ch <= 'Z'; ch++) {
        printf("%c ", ch);
    }

    printf("\n");

    return 0;
}